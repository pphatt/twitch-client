import styled from "styled-components"

export const ChannelVideoPlaceholderWrapper = styled.div`
  position: relative;

  width: 100%;

  overflow: hidden;
`

export const ChannelVideoPlaceholderContainer = styled.div`
  padding-bottom: 56.25%;
`

export const ChannelVideoPlaceholderLayout = styled.div`
  position: absolute;
  inset: 0;

  width: 100%;
  min-height: 100%;
`
