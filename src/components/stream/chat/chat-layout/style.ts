import styled from "styled-components"

export const ChatRoomComponentLayout = styled.section`
  display: flex;
  flex-direction: column;
  flex-shrink: 1;
  flex-grow: 1;

  background: hsl(var(--background));

  width: 100%;
  height: 100%;
  min-height: 0;
`
