import styled from "styled-components"

export const FormGroupLabelWrapper = styled.div`
  flex-shrink: 0;

  width: 180px;

  padding-right: 20px;
`

export const FormGroupLabelContainer = styled.div`
  margin-bottom: 0.5rem;
`

export const FormGroupLabel = styled.label`
  color: hsl(var(--color-hinted-grey-15));

  margin-left: 0;

  font-weight: 700;
`
