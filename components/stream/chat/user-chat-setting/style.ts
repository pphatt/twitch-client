import styled from "styled-components"

export const ChatSettingsContent = styled.div`
  color: hsl(var(--foreground));
  background-color: hsl(var(--background));

  border-bottom-left-radius: 4px;
  border-bottom-right-radius: 4px;

  padding: 10px;
`
